apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  name: bank-admin-vault-auth-cluster-admin-role-binding
  namespace: bank-ns
  labels:
    organization: bank
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: bank-admin-vault-auth
  namespace: bank-ns
