apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: university-university-steward-1-node
  annotations:
    fluxcd.io/automated: "false"
  namespace: university-ns
spec:
  releaseName: university-university-steward-1-node
  chart:
    path: platforms/hyperledger-indy/charts/indy-node
    git: https://github.com/sauveergoel/blockchain-automation-framework.git
    ref: indydemo
  values:
    metadata:
      name: university-university-steward-1-node
      namespace: university-ns
    network:
      name: baf
    organization:
      name: university
    genesis:
      pool: Z2VuZXNpcy9wb29sX3RyYW5zYWN0aW9uc19nZW5lc2lz
      domain: ZG9tYWluL2RvbWFpbl90cmFuc2FjdGlvbnNfZ2VuZXNpcw==
      add_org: False
    image:
      pullSecret: regcred
      initContainer:
        name: university-university-steward-1-node-init
        repository: alpine:3.9.4
      cli:
        name: university-university-steward-1-node-ledger-txn
        repository: index.docker.io/hyperledgerlabs/indy-ledger-txn:latest
        pullSecret: regcred  
      indyNode:
        name: university-university-steward-1-node
        repository: index.docker.io/hyperledgerlabs/indy-node:1.11.0
    node:
      name: university-steward-1
      ip: 0.0.0.0
      publicIp: 54.216.102.224
      port: 9711
      ambassadorPort: 15111
    client:
      publicIp: 54.216.102.224
      ip: 0.0.0.0
      port: 9712
      ambassadorPort: 15112
    service:
      type: ClusterIP
      ports:
        nodePort: 9711
        nodeTargetPort: 9711
        clientPort: 9712
        clientTargetPort: 9712
    configmap:
      indyConfig: |-
        NETWORK_NAME = 'baf'
        # Enable stdout logging
        enableStdOutLogging = True
        logRotationBackupCount = 10
        # Directory to store ledger.
        LEDGER_DIR = '/var/lib/indy/data'
        # Directory to store logs.
        LOG_DIR = '/var/log/indy'
        # Directory to store keys.
        KEYS_DIR = '/var/lib/indy/keys'
        # Directory to store genesis transactions files.
        GENESIS_DIR = '/var/lib/indy/genesis'
        # Directory to store backups.
        BACKUP_DIR = '/var/lib/indy/backup'
        # Directory to store plugins.
        PLUGINS_DIR = '/var/lib/indy/plugins'
        # Directory to store node info.
        NODE_INFO_DIR = '/var/lib/indy/data'
    ambassador:
      annotations: |-
        ---
        apiVersion: ambassador/v1
        kind: TCPMapping
        name: university-university-steward-1-node-node-mapping
        port: 15111
        service: university-university-steward-1-node.university-ns:9711
        ---
        apiVersion: ambassador/v1
        kind: TCPMapping
        name: university-university-steward-1-node-client-mapping
        port: 15112
        service: university-university-steward-1-node.university-ns:9712
    vault:
      address: http://vault.internal.demo.aws.blockchaincloudpoc.com:9001
      serviceAccountName: university-university-steward-1-vault-auth
      keyPath: /keys/baf/keys/university-steward-1
      authPath: kubernetes-university-university-steward-1-auth
      nodeId: university-steward-1
      role: ro
    storage:
      data:
        storagesize: 3Gi
        storageClassName: university-aws-storageclass
      keys:
        storagesize: 3Gi
        storageClassName: university-aws-storageclass
